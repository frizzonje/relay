-- Отдельная база под тесты api. Postgres выполняет всё из этого каталога один
-- раз, при создании тома, — и только в dev-стеке: в проде такой базы нет и быть
-- не должно.
--
-- Имя обязано заканчиваться на _test: проверка схемы вычищает базу целиком и
-- отказывается работать с любой другой (см. apps/api/src/db/schema.test.ts).
CREATE DATABASE relay_test OWNER relay;
