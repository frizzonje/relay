/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // @relay/shared отдаётся как TS-исходник — Next компилирует его сам.
  transpilePackages: ['@relay/shared'],
  // Минимальный runtime для прод-образа.
  output: 'standalone',
  /**
   * Заголовки, которых не было ни одного. Все четыре — постоянные строки: ничего
   * не считают, ничего не запрашивают, на время ответа не влияют.
   *
   * Главный здесь — запрет обрамления. Права на реестровые записи вязаны к
   * устройству (audit B2), то есть удаление чужого сервера — это один клик в
   * браузере владельца, а страница входа принимает пароль. И то, и другое —
   * ровно то, ради чего чужой сайт открывает нас в невидимом фрейме. Своих
   * фреймов у нас нет нигде: web не содержит ни одного iframe, десктоп уводит
   * webview на адрес верхним переходом (clients/desktop/src/main.js).
   * `frame-ancestors` понимают современные браузеры, X-Frame-Options — старые.
   *
   * Чего здесь сознательно НЕТ:
   *   - CSP на скрипты: Next гидрирует инлайном, и честный script-src требует
   *     нонса на каждый запрос — работа на горячем пути и риск белого экрана;
   *   - HSTS: инсталляция на самоподписанном сертификате или на голом IP —
   *     штатный режим (см. install.sh), а HSTS у части браузеров отнимает у
   *     человека возможность пройти мимо предупреждения. Его место — Caddy на
   *     установке с настоящим доменом, а не общий образ;
   *   - Permissions-Policy: у нас микрофон, камера и захват экрана — это и есть
   *     продукт. Один неверный список глушит звонок целиком.
   *
   * Живёт в образе, а не в Caddyfile, намеренно: файлы стека до существующих
   * инсталляций не доезжают (audit B7), а образ доезжает обычным relay update.
   */
  async headers() {
    return [
      {
        source: '/:path*',
        headers: [
          { key: 'Content-Security-Policy', value: "frame-ancestors 'none'" },
          { key: 'X-Frame-Options', value: 'DENY' },
          { key: 'X-Content-Type-Options', value: 'nosniff' },
          { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
        ],
      },
    ];
  },
};

export default nextConfig;
