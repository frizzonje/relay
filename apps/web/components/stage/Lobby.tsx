'use client';

import { ServerStats } from '@/components/stage/ServerStats';

/**
 * Лобби — то, что видно на главном экране, пока не открыт ни один канал.
 *
 * Здесь стоит состояние машины, на которой живёт инсталляция (процессор,
 * память, диск): relay ставят на свой сервер, и первое, что о нём хочется
 * знать, — как он себя чувствует. Быстрый вход по коду, живший здесь раньше,
 * переехал в сайдбар («по коду») и в мобильный bottom sheet — там он ближе к
 * списку каналов, среди которых его и ищут.
 */
export function Lobby() {
  return <ServerStats />;
}
